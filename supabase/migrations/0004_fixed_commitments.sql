-- ============================================================================
--  الالتزامات الثابتة (خطة §4.2): مدرسة، دروس خصوصية، نوم...
--
--  تُخزَّن كفترات جدول من نوع 'fixed' — نفس جدول schedule_slots ليستفيد من
--  بنية local-first والمزامنة الجاهزة. الجدولة التلقائية/المساعدة تتجنّبها،
--  ولا تتولّد منها مهام يومية، وتُدار من الإعدادات (قابلة للتعديل دائماً).
-- ============================================================================

alter table schedule_slots
  add column if not exists slot_type text not null default 'study'
    check (slot_type in ('study','fixed'));
